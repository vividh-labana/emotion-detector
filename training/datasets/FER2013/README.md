Place the FER2013 raw files here.

Accepted inputs:
- `fer2013.csv` (original CSV from Kaggle) **or**
- `train/` and `test/` directories containing class-named folders.

Then run `python ../../prepare_fer2013.py --raw-dir . --out-dir ../images` from this directory.